name: Deploy Dashboard to S3

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-central-1

      - name: Deploy to S3
        run: |
          aws s3 sync ./dashboard s3://my-cloud-cost-dashboard \
            --delete \
            --exact-timestamps \
            --exclude "data/cost_data.json"
          find dashboard -name '*.html' | while read -r f; do
            key="${f#dashboard/}"
            aws s3 cp "$f" "s3://my-cloud-cost-dashboard/$key" \
              --content-type "text/html; charset=utf-8" \
              --metadata-directive REPLACE
          done

      - name: Invoke Lambda to generate cost_data.json
        run: |
          aws lambda invoke \
            --function-name fetch_cloud_costs \
            --region eu-central-1 \
            --cli-binary-format raw-in-base64-out \
            response.json
          cat response.json

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id E1GC2UYUFIC3YM \
            --paths "/data/cost_data.json" "/index.html" "/*"

